DEPENDENCIES

  ocaml
  camlp4/ocaml-camlp4/ocaml-camlp4-devel
  flex
  bison
  indent
  ncurses-devel
  emacs

INSTALL

  ./configure && make

RUN

  s2s --arch=<x86/cell> [options] <file> [file2 ...]

OPTIONS

  --arch  Defines the target architecture (x86/cell)
          MUST BE THE FIRST ARGUMENT

  --debug
          Enable debugging information printing.

  --out-name
          Specify the output files' prefix.
          e.g. (default: final) will produce 
          final.c and final_func.c for the cell architecture
#FIXME#          and final.c? for the x86 architecture

#FIXME# to god x86 exei queue?
  --queue-size
          Specify the queue size for Cell. The value must much the one defined
          in the Makefile as MAX_QUEUE_ENTRIES

  --block-size
          Specify the block size for x86.  The value must much the one defined
          in the Makefile as BLOCK_SZ

  --with-stats
          Enable code for statistics (for use with -DSTATISTICS)

  --with-unaligned-arguments
          Allow unalligned arguments in x86
          (for use with -DUNALIGNED_ARGUMENTS_ALLOWED)

  --threaded
          Generate thread safe code (for use with -DTPC_MULTITHREADED)