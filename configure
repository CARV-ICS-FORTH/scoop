#!/bin/sh
ROOT=`pwd`
cd cil
#autoconf
./configure EXTRASRCDIRS="$ROOT/src" \
  EXTRAFEATURES="scoop"
cd ..
files="locksmith lockpick scoop"
for i in $files
do
	cat $i.in | sed -e "s.ROOT.$ROOT." > $i
done
chmod a+x $files
