#!/bin/sh
ROOT=`pwd`
cd cil
#autoconf
./configure EXTRASRCDIRS="$ROOT/src" \
  EXTRAFEATURES="scoop"
cd ..
for i in *.in
do
	sed 's.ROOT.'$ROOT'.g' $i > ${i%.in}
	chmod a+x ${i%.in}
done
if ! grep $ROOT $HOME/.bashrc > /dev/null
then
	echo 'export PATH=$PATH:'$ROOT >> $HOME/.bashrc
fi
